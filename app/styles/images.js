import { StyleSheet } from 'react-native';

// image styles

const styles = StyleSheet.create({
	autoWidth: {
		width: "100%",
		height: "100%"
	},

	round: {
		borderRadius: 100
	},


	// whotModal card
	whotCard: {
		width: 65,
		height: 83,
	}
});

export default styles;